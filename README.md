# نظام إدارة المطعم الاحترافي 🍔

نظام متكامل للمطاعم يتيح للعملاء تصفح المنيو والطلب عبر واتساب، مع لوحة تحكم شاملة للأدمن.

## 🚀 المميزات
- **واجهة مستخدم عصرية**: تصميم متجاوب (Responsive) مع دعم كامل للغة العربية.
- **سلة مشتريات ذكية**: إضافة وتعديل الكميات بسهولة.
- **طلب عبر واتساب**: إرسال تفاصيل الطلب والإجمالي مباشرة للمطعم.
- **لوحة تحكم (Admin Panel)**:
  - إضافة وتعديل وحذف المنتجات.
  - إدارة أقسام المنيو.
  - متابعة الطلبات وتحديث حالتها (جديد، جاري التحضير، تم التسليم).
- **قاعدة بيانات Firebase**: تحديثات فورية (Real-time) للمنتجات والطلبات.

## 🛠️ التعليمات للتشغيل

### 1. إعداد Firebase
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/).
2. أنشئ مشروعاً جديداً.
3. أضف تطبيق ويب (Web App) وانسخ إعدادات الـ Config.
4. افتح ملف `js/firebase-config.js` وألصق الإعدادات الخاصة بك.
5. في قائمة Firebase:
   - قم بتفعيل **Firestore Database** (أنشئ قاعدة البيانات في وضع Testing).
   - قم بتفعيل **Authentication** وفعل خيار "Email/Password".

### 2. إضافة الأدمن
1. في قسم **Authentication**، أضف مستخدماً جديداً (إيميل وباسورد).
2. استخدم هذه البيانات لتسجيل الدخول إلى لوحة التحكم عبر `admin.html`.

### 3. إعداد رقم واتساب
1. افتح ملف `js/app.js`.
2. ابحث عن المتغير `phoneNumber` (السطر 164 تقريباً).
3. استبدله برقم هاتفك (مثال: `201012345678`).

## 📁 هيكل المشروع
- `index.html`: الواجهة الرئيسية للعملاء.
- `admin.html`: لوحة تحكم المشرف.
- `js/app.js`: منطق تشغيل الموقع (سلة المشتريات، المنيو).
- `js/admin.js`: منطق لوحة التحكم (إضافة المنتجات، الطلبات).
- `css/style.css`: التصميم الموحد للموقع.
- `images/`: مجلد الصور.
